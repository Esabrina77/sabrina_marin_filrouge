spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  sql:
    init:
      mode: never

application:
  security:
    jwt:
      secret-key: test_secret_key_which_is_long_enough_for_the_algorithm_123456
      expiration: 3600000
      refresh-token:
        expiration: 86400000
